---
layout: post
title:  函数
categories: [日常, 学习, C]
tags: [C]
description: 函数,多文件编译,递归,goto
typora-root-url: ../
---

# 函数

## 函数的定义和使用

- 函数是执行特定任务的代码块。它可以接收输入（称为参数），完成任务后返回结果

- 声明:

  ```c
  //返回类型 函数名(参数类型 参数名, ...);
  int add(int a, int b); // 声明一个名为 add 的函数，返回值是 int 类型，接收两个 int 类型的参数
  ```

- 定义:

  ```c
  /*
  返回类型 函数名(参数类型 参数名, ...) {
  // 函数体，执行的操作
  return 返回值; // 如果有返回值
  }
  */
  int add(int a, int b) {
  	return a + b;
  }
  ```

- 调用

  ```c
  //在 main() 函数或其他函数中，通过函数名调用函数，并传递参数。
  int result = add(5, 10); // 调用 add 函数，传递 5 和 10 两个参数
  ```

- 参数

  - 形参

  > 形参是函数定义中声明的参数，它是函数接收数据的占位符，用于描述函数在执行时将会使用的变量。
  > 形参是在函数的定义部分给出的，代表函数调用时将传入的值，但本身在函数定义时没有具体的值。

  ```c
  int add(int a, int b) { // 这里 a 和 b 是形参
  	return a + b;
  }
  ```

  - 实参

  > 实参是函数调用时传递给形参的实际值或变量。实参为函数执行提供了具体的输入。当调用函数时，实参的值会被复制给形参，函数执行时使用这些值。

  ```c
  int result = add(5, 10); // 这里 5 和 10 是实
  ```

  - 形参与实参的关系

  > 在函数调用过程中，实参的值被按值传递给形参。这意味着实参的值被复制到形参中，函数内的任何操作都不会影响到实参本身。
  > 换句话说，形参的变化不影响实参，除非使用指针或特殊方式传递

## 头文件与函数的关系

> 头文件与函数的关系主要体现在函数声明上。头文件通常包含了函数的声明，而这些声明会在源文件中被包含（通过 #include），使得其他文件可以调用这些函数。

## 错误情况

- 函数为声明或声明不一致
- 返回类型错误或没有匹配的return语句
- 函数参数不匹配
- 返回局部变量的地址

# 多文件编译

task.json 配置:	-g 所有.c文件 -I include目录

c_cpp_properties.json 也需要添加include目录

# 递归函数

- 递归函数的基本原理

> 递归函数通过重复调用自身来解决问题。每个递归函数至少需要两个部分：
> 基线条件（Base Case）：递归终止的条件，当满足该条件时，递归停止，避免无限循环。
> 递归步骤（Recursive Step）：在每次递归调用时，问题被分解为一个或多个更小的问题。

- 递归函数的结构

```c
//递归函数的基本结构如下：
return_type function_name(parameters) {
	if(base_case_condition) {
	// 基线条件，终止递归
	return	base_case_value;
	} else {
// 递归步骤，继续调用函数自身
	return	function_name(smaller_problem);
	}
}
```

- 示例:

```c
//阶乘计算
#include<stdio.h>
int	factorial(intn) {
	if(n == 0|| n == 1) {// 基线条件：n为0或1时，返回1
		return1;
	}else{
		return	n * factorial(n -1);// 递归步骤
	}
}
int	main() {
	int	number = 5;
	printf("Factorial of %d is %d\n", number, factorial(number));
	return0;
}

//斐波那契数列
#include <stdio.h>

int fibonacci(int n){
    if(n <= 1){
        return n;
    }
    return fibonacci(n-1) + fibonacci(n-2);
}
int main(void){
    printf("%d\n", fibonacci(5));
    return 0;
}
```

- 递归与迭代的比较

> 递归与迭代（如for或while循环）在解决问题时经常可以相互替代。两者的主要区别如下：
> 递归：
> 自然适用于问题本身具有递归性质的场景。
> 容易实现，但可能导致栈溢出，性能开销大。
> 迭代：
> 循环结构往往更加高效。
> 代码可能不如递归表达直观，尤其是当问题本身是递归时。

# goto语句

- 语法

```c
goto label; // 直接跳转到标签位置
...
label: // 定义标签位置
// 代码块
//goto只能在同意函数内部跳转, 不能跨函数跳转
```

- 示例

```c
//跳出多层循环
#include <stdio.h>
int main() {
	inti, j;
    for(i= 0; i< 5; i++) {
		for(j = 0; j < 5; j++) {
			if(i== 2&& j == 3) {
				goto exit_loop;// 当满足条件时跳出所有循环
			}
			printf("i= %d, j = %d\n", i, j);
		}
	}
exit_loop:
	printf("Exited loops at i= %d, j = %d\n", i, j);
	return0;
}
```

# 错误分析

- goto语句通常用于跳过某段代码并继续执行

- 在堆上分配的内存超过系统允许的限额并不会导致栈溢出....

- 静态函数只能在声明它的文件内使用 ? 如果这个函数在外部静态定义, 本文件仅作声明能否使用?

  ![static声明函数](/assets/pictures/static声明函数.png)

- goto语句允许跳转到任意标签位置, 包括函数开始, 如何定义函数开始?

- #include 同样可以用于.c文件的引入, 只不过不推荐.
